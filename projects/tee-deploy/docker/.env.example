# Generate these with: node src/deploy.js --dry-run --anthropic-key YOUR_KEY --telegram-token YOUR_TOKEN --telegram-user YOUR_ID
#
# The deploy tool outputs the base64 values. Paste them here.

OPENCLAW_CONFIG_B64=<base64-encoded openclaw.json>
OPENCLAW_AUTH_B64=<base64-encoded auth-profiles.json>
